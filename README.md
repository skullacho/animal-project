#PEDRO SANTE MARIA BUENO SILVA

#Nessa atividade eu implementei o enum FurColor e a classe Cat usando como base os arquivos já prontos do projeto, principalmente Dog.java e TrainingLevel.java.

#No FurColor, eu criei as constantes das cores dos gatos com código e nome de exibição. Também fiz os getters e o método `fromCode()`, que procura a cor pelo código informado.

#Na classe Cat, eu usei herança com a classe Animal e adicionei os atributos pedidos na atividade, como vidas, cor do pelo, comida favorita e frequência do ronronado.

#Também sobrescrevi os métodos makeSound(), sleep() e displayInfo(), além de criar os overloads do método move().

#As decisões que tomei foram seguir o mesmo padrão do projeto para deixar o código organizado e parecido com o restante das classes já existentes.

#Para testar, eu criei objetos Cat no main() e chamei os métodos para verificar se tudo estava funcionando corretamente.
